# Environment / secrets
.env
.env.*
!.env.example
*.local

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# Node
node_modules/
npm-debug.log*
yarn-error.log*

# Spark scratch / data outputs (keep the folder, ignore contents)
spark/data/*
!spark/data/.gitkeep

# Job code backups (keep the folder, ignore contents)
backups/*
!backups/.gitkeep

# Archived code of deleted jobs (keep the folder, ignore contents — persisted on a volume in prod)
archive/*
!archive/.gitkeep
*.parquet
*.log

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
